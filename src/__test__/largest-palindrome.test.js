import { largestPalindrome } from '../largest-palindrome';

it(`should find the largest palindrome made
 from the product of two 3-digit numbers.`, () => {
  expect(largestPalindrome()).toBe(906609)
});
 